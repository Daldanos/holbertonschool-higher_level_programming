# Test cases for BaseGeometry class

# Test 1: area() method raises exception
try:
    base_geometry = BaseGeometry()
    base_geometry.area()
except Exception as e:
    print(e)  # Expected output: area() is not implemented

# Test 2: integer_validator() with valid integer
base_geometry.integer_validator("width", 5)  # No exception expected

# Test 3: integer_validator() with non-integer value
try:
    base_geometry.integer_validator("height", 3.14)
except TypeError as e:
    print(e)  # Expected output: height must be an integer

# Test 4: integer_validator() with zero value
try:
    base_geometry.integer_validator("base", 0)
except ValueError as e:
    print(e)  # Expected output: base must be greater than 0

# Test 5: integer_validator() with negative value
try:
    base_geometry.integer_validator("radius", -2)
except ValueError as e:
    print(e)  # Expected output: radius must be greater than 0